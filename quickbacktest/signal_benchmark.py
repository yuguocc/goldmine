"""AgentEvaluator Template"""

from src.environment.quickbacktest.base_types import BaseSignalBenchmark
import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
from pathlib import Path


class AgentSignalEvaluator(BaseSignalBenchmark):
    """
    AgentSignalEvaluator
    ==============

    This class is defined as a template for evaluating trading signals generated by an agent.

    IMPORTANT DESIGN PRINCIPLE

    A single benchmark should test **one specific research hypothesis** about the signal,
    rather than performing a full analysis of the signal.

    The goal of this evaluator is to validate whether a particular assumption about the signal
    is supported by the data so that the signal design can be improved later.

    Examples of hypotheses include:

        - The signal contains directional predictive information
        - The signal is more effective during high volatility periods
        - The signal behaves differently in high liquidity environments
        - The signal exhibits persistence across time
        - The signal reacts differently under certain market conditions

    Therefore, the evaluation logic should be designed as a **targeted experiment**
    rather than a general statistical summary.

    Data access as initialized in BaseSignalBenchmark as:

        self.close: pd.DataFrame = self.pivot_frame["close"]
        self.volume: pd.DataFrame = self.pivot_frame["volume"]
        self.open: pd.DataFrame = self.pivot_frame["open"]
        self.high: pd.DataFrame = self.pivot_frame["high"]
        self.low: pd.DataFrame = self.pivot_frame["low"]
        self.amount: pd.DataFrame = self.pivot_frame["amount"]
        self.signal_1: pd.DataFrame = self.pivot_frame["signal_1"]
        self.signal_2: pd.DataFrame = self.pivot_frame["signal_2"]
        self.signal_3: pd.DataFrame = self.pivot_frame["signal_3"]
        self.signal_4: pd.DataFrame = self.pivot_frame["signal_4"]
        self.signal_5: pd.DataFrame = self.pivot_frame["signal_5"]

    The evaluator should focus only on metrics that are relevant to the hypothesis being tested.
    Evaluation metrics are not fixed and can be **custom designed** depending on the experiment.
    """

    def numeric_evaluation(self):
        """
        The method is defined as a placeholder for the numeric evaluation logic.

        The evaluation should implement a **hypothesis-driven test** for the signals.

        Instead of performing a broad signal analysis, the benchmark should:

            1. Define the hypothesis being tested
            2. Design the numeric evaluation logic specifically for that hypothesis
            3. Compute only the metrics required to validate that hypothesis

        The output should be returned as a dictionary summarizing the numeric evaluation results.

        Example output format:

        {
            "signal_1": {
                "metric_1": value,
                "metric_2": value,
                "other_metric": value
            },
            "signal_2": {
                "metric_1": value,
                "metric_2": value,
                "other_metric": value
            },
            "signal_3": {
                "metric_1": value,
                "metric_2": value,
                "other_metric": value

            ...
            }
        }

        The exact metrics should be determined by the hypothesis being tested.
        """
        raise NotImplementedError


    def plot_evaluation(self):
        """
        The method is defined as a placeholder for the plotting evaluation logic.

        The plots should be designed specifically to provide visual evidence
        for the same single hypothesis being tested in `numeric_evaluation`.

        Instead of generating a full collection of generic signal plots,
        the benchmark should only produce figures that are directly relevant
        to the hypothesis.

        IMPORTANT REQUIREMENT

        The base directory of the experiment is available as:

            self.base_dir

        All generated figures **must be saved under**:

            {self.base_dir}/images/{name}.png

        Only images saved in this directory will be detected and displayed by the system.

        Example implementation:

            image_dir = Path(self.base_dir) / "images"
            image_dir.mkdir(parents=True, exist_ok=True)

            plt.figure()
            ...
            plt.savefig(image_dir / "example_plot.png")

        Example of a valid hypothesis-relevant plot:

            If the hypothesis is that a signal can distinguish trending regimes
            from ranging regimes, the plot can visualize the close series and
            color different time segments according to a regime classification
            derived from `signal_1`, `signal_2`, signal_3`, `signal_4` or `signal_5`.

            For example:
                - use one color for periods classified as trend
                - use another color for periods classified as range
                - overlay these colored segments on the close price series
                - include a clear legend
                - save the figure with a filename related to the hypothesis,
                  such as:
                      trend_range_regime_test.png

            This kind of plot is encouraged because it provides direct visual
            evidence for the hypothesis rather than a generic chart.

        Multiple plots can be generated if needed, but each plot must follow
        the `{self.base_dir}/images/{name}.png` convention.
        """
        raise NotImplementedError